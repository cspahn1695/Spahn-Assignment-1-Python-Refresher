# Spahn-Assignment-1-Python-Refresher<img width="750" height="571" alt="image" src="https://github.com/user-attachments/assets/3ce62a07-08f3-420c-a576-11adaed96747" />

This image shows how fib(n) and the time to compute it increase somewhat (but perfectly) linearly as n increases. The spikes result from python big-integer sizing and cache growth / memory allocator behavior. You can't remove the spikes while still measuring real python fibonacci integers - since the spikes are real work being done. Additionally, it can be seen that the times to compute the fib(n) values for n = 1-100 are much smaller than in the example provided with the assignment. It is likely the example graph provided with the assignment didn't using caching.
